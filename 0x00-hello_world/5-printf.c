#include<stdio.h>
/**
  * main - prints a line
  * Return: always returns 0
  */

int main(void)
{
	char string1[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", string1);
	return (0);
}
